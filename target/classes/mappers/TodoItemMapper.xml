<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demotodo.mapper.TodoItemMapper">
    <select id="selectTodoItems" resultType="com.demotodo.dto.TodoItemDTO">
        select * from todoitems;
    </select>
    
    <!-- <insert id="insertTodoItems" parameterType="com.demotodo.dao.TodoItemDTO">
    	insert into todoitems (
    		item_user,
    		item_todo
    	) values (
    		#{item_user},
    		#{item_todo}
    	);
    </insert> -->
</mapper>